spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/jay
    username: jay
    password: 1234
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
server:
  port: 9000
logging:
  level:
    root: debug
